{% extends 'back.html.twig' %}

{% block title %}
    ✏️ Modifier Objectif
{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">

                <div class="card shadow-lg border-0 rounded-4 overflow-hidden">

                    {# En-tête stylisée #}
                    <div class="card-header bg-gradient bg-warning text-dark py-4 px-5">
                        <div class="d-flex justify-content-between align-items-center">
                            <h3 class="mb-0 fw-bold">
                                <i class="fas fa-edit me-2"></i>Modifier l'objectif
                            </h3>
                            <a href="{{ path('app_objectif_index') }}" class="btn btn-outline-light btn-sm">
                                <i class="fas fa-arrow-left me-1"></i>Retour
                            </a>
                        </div>
                    </div>

                   
                    <div class="card-body bg-white px-5 py-4">
                        {{ include('objectif/back/_form.html.twig', { 'button_label': '💾 Mettre à jour' }) }}
                    </div>

                    {# Pied avec suppression #}
                    <div class="card-footer bg-light px-5 py-3 text-end">
                        <div class="d-inline-block">
                            {{ include('objectif/back/_delete_form.html.twig') }}
                        </div>
                    </div>

                </div>

            </div>
        </div>
    </div>
{% endblock %}

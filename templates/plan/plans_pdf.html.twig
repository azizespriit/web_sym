<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Plans - {{ objectif.nom }}</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #3498db;
            padding-bottom: 10px;
        }
        .header h1 {
            color: #2c3e50;
            margin-bottom: 5px;
        }
        .header .objectif {
            font-size: 1.2rem;
            color: #3498db;
            font-weight: bold;
        }
        .plan {
            border: 1px solid #e0e0e0;
            margin-bottom: 15px;
            padding: 15px;
            border-radius: 8px;
            background-color: #f9f9f9;
            page-break-inside: avoid;
        }
        .plan h2 {
            margin: 0 0 10px 0;
            color: #e74c3c;
            font-size: 1.3rem;
            border-bottom: 1px dashed #ddd;
            padding-bottom: 5px;
        }
        .plan p {
            margin: 8px 0;
            padding-left: 10px;
        }
        .label {
            font-weight: bold;
            color: #16a085;
        }
        .value {
            color: #7f8c8d;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            font-size: 0.8rem;
            color: #95a5a6;
            border-top: 1px solid #eee;
            padding-top: 10px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Liste des Plans d'Entraînement</h1>
        <div class="objectif">Objectif : {{ objectif.nom }}</div>
    </div>

    {% for plan in plans %}
        <div class="plan">
            <h2>Jour {{ plan.Jour }}</h2>
            
            <p>
                <span class="label">Nutrition :</span>
                <span class="value">{{ plan.Nutration }}</span>
            </p>
            
            <p>
                <span class="label">Muscle :</span>
                <span class="value" style="color: #e67e22;">{{ plan.Muscle }}</span>
            </p>
            
            <p>
                <span class="label">Course :</span>
                <span class="value" style="color: #9b59b6;">{{ plan.Course }}</span>
            </p>
        </div>
    {% endfor %}

    <div class="footer">
        Généré le {{ "now"|date("d/m/Y à H:i") }} - Programme d'entraînement
    </div>
</body>
</html>